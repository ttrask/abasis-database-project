@{
    Layout = null;
}
@using COP4710.Models;
@using COP4710.Models.Enumerations;
<!DOCTYPE html>
@if (Session["CurrentUser"] != null && ((UserModel)Session["CurrentUser"]).AccountType == AccountType.Administrator)
{<div style="display: inline-block;">
    <div style="display: block;">
        <div class="MenuItem" id="Users">
            @Html.ActionLink("Users", "Index", "User", null, new { })
        </div>
        <div class="MenuItem" id="Forms">
            @Html.ActionLink("Forms", "Index", "Dispatch", null, new { })
        </div>
        <div class="MenuItem" id="Reports">
            @Html.ActionLink("Reports", "Index", "Report", null, new { })
        </div>
    </div>
    </div>
}